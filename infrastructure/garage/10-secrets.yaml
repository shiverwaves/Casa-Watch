# WARNING: Plaintext lab credentials committed to Git. Do NOT use beyond the lab.
# Regenerate before any non-toy use:
#   openssl rand -hex 32       (rpc-secret: must be 64 hex chars)
#   openssl rand -base64 32    (admin-token, metrics-token)
# DESIGN.md targets sealed-secrets day-2; this is day-1.
apiVersion: v1
kind: Secret
metadata:
  name: garage-secrets
  namespace: storage
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  rpc-secret: "deadbeefcafebabe1234567890abcdef0123456789abcdefdeadbeefcafebabe"
  admin-token: "DEV-ONLY-replace-with-openssl-rand-base64-32-before-anything-real"
  metrics-token: "DEV-ONLY-replace-with-openssl-rand-base64-32-before-anything-real"
